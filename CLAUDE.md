# Echo of Photons 项目协作入口

本文件是 Echo of Photons 项目的项目级协作入口。
全局协作规范仍以用户主目录下的全局 `CLAUDE.md` 为准；本文件只补充当前项目的定位、文档入口、执行边界与阶段约束。

## 1. 项目定位

Echo of Photons 是一款以星图识别为入口，帮助用户看懂夜空并逐步建立观星兴趣的应用。

第一版 MVP 聚焦三类对象：
- 星座
- 亮星
- 行星

第一版主闭环：
- 首页推荐
- 星图识别
- 详情解释卡片
- 继续探索

它不是纯百科，也不是纯专业观测工具。

## 2. 执行前必须阅读的文档

开始任何实现、修改、验证或审查前，先阅读：
- `docs/research.md`
- `docs/requirements.md`
- `docs/tech-spec.md`
- `docs/design-spec.md`
- `docs/api-contract.md`
- `docs/dev-plan.md`
- 本文件 `CLAUDE.md`

如果任务非常小，也不能跳过：
- `docs/dev-plan.md`
- `docs/api-contract.md`（如任务涉及接口）
- `docs/tech-spec.md`（如任务涉及数据、后端、目录职责）

## 3. 文档优先级

如有冲突，按以下顺序执行：
1. `requirements.md`
2. `tech-spec.md`
3. `api-contract.md`
4. `design-spec.md`
5. `dev-plan.md`
6. 本文件 `CLAUDE.md`

说明：
- `requirements.md` 决定做什么、不做什么
- `tech-spec.md` 决定技术路线和数据主线
- `api-contract.md` 决定接口边界与返回结构
- `design-spec.md` 决定页面职责、信息层级与交互方向
- `dev-plan.md` 决定当前阶段该做什么

## 4. 当前技术路线

当前项目锁定技术主线：
- Next.js
- React
- TypeScript
- Tailwind CSS
- PostgreSQL
- Next.js 内聚式轻后端
- `pg` 作为当前阶段数据库访问方式

未明确批准前，不允许：
- 引入 ORM
- 更换数据库
- 更换前端框架
- 重写项目目录主结构
- 擅自扩展成重后端或重中台架构

## 5. 当前目录职责

- `app/`：页面路由与 API 路由入口
- `components/`：跨页面复用 UI 组件
- `features/`：按业务领域组织的前端功能模块
- `server/`：后端业务组织层，承接后续服务逻辑
- `data-access/`：数据库访问层
- `lib/`：轻量通用工具、响应结构、env 读取等
- `docs/`：项目真源文档

当前阶段不要随意扩展新的顶层目录。

## 6. 项目执行规则

1. 严格按 `docs/dev-plan.md` 的阶段顺序推进
2. 一次只执行一个最小单一任务，不跨阶段扩展
3. 如实现想法与文档冲突，以文档为准
4. 不允许自行补充未确认需求
5. 不允许自行改动接口契约、数据库主线和页面职责
6. 先完成当前最小任务，再停下等待审查
7. 未经阶段审查通过，不进入下一阶段

## 7. 前后端执行顺序

本项目属于全栈项目，默认顺序为：
- UI
- 数据库
- 后端

含义：
- 先明确页面职责和数据形态
- 再固定数据库结构
- 最后用后端把页面和数据接起来

不要跳过数据库直接进入完整业务后端。

## 8. 接口约束

统一返回结构固定为：

```json
{
  "code": 0,
  "data": {},
  "message": "ok"
}
```

错误返回也必须保持同一结构，只允许 `code`、`data`、`message` 的值变化，不允许私自改 key。

当前阶段中：
- `/api/recommendations`、`/api/objects/[slug]`、`/api/tools/observation-summary` 为正式契约
- `/api/sky-map/resolve` 为预契约

不要把预契约接口提前写死成最终字段形态。

## 9. 审查机制

本项目有两层审查：

### 最小任务审查
每个最小单一任务结束后：
- 先输出完成报告
- 停下等待审查
- 审查通过后再进入下一个最小任务

### 阶段审查
每个阶段结束后：
- 必须做阶段级代码审查
- 阶段级审查会直接查看真实代码与实现结果
- 不能用文字汇报替代代码事实
- 未经阶段审查通过，不进入下一阶段

## 10. 输出要求

开始执行前先复述：
- 当前项目定位
- 当前所处阶段
- 本轮要做什么
- 本轮明确不做什么
- 将遵守哪些文档约束

完成后输出：
- 本轮完成内容
- 对应 `dev-plan.md` 的哪一条
- 修改了哪些文件
- 本轮明确没有做什么
- 验证方式和结果
- 是否存在阻塞项或待确认点

## 11. 本文件的角色边界

本文件不是用来重写全局工作流的。
它只负责让进入这个仓库的执行者快速知道：
- 这是什么项目
- 真源文档在哪
- 当前技术路线是什么
- 应该如何按阶段推进
- 不能擅自改什么

如本文件与全局 `CLAUDE.md` 冲突，以全局规范和项目 docs 的高优先级文档为准。
